# conda environment description file for docs build envioronment
# on readthedocs.org

name: sphinx-build

channels:
  - conda-forge
  - nodefaults

dependencies:
  - pip
  - python=3.10
  - pip:
      - pyramid
      - sentry_sdk
