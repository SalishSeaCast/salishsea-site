name: Assign Issue/PR

permissions: {}

on:
  issues:
    types:
      - reopened
      - opened
  pull_request:
    types:
      - reopened
      - opened

jobs:
  auto_assign:
    permissions:
      issues: write  # Required to add an assignee to an issue
      pull-requests: write  # Required to add an assignee to a pull request
    uses: UBC-MOAD/gha-workflows/.github/workflows/auto-assign.yaml@main  # zizmor: ignore[unpinned-uses]
